# Production docker-compose configuration
# NOTE: SSL termination is handled by design_workflow_frontend nginx (single reverse proxy)
services:
  web:
    build: .
    container_name: design_workflow_frontend_web
    restart: always
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
    extra_hosts:
      - "design-workflow-api.elbouazzatiholding.ma:host-gateway"
    ports:
      - '3005:3004'
    networks:
      - webnet
    healthcheck:
      test: ["CMD-SHELL", "pgrep -f bun || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 15s

networks:
  webnet:
    driver: bridge
